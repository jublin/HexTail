# Offline-installable HexTail PWA tool

1. Add the standard Blazor PWA manifest, icons, and service workers.
2. Add `HexTailSharp.Tool`, a loopback-only `hextail` .NET tool that packs the published client.
3. Add option tests, package/install verification, license, and offline-install instructions.
