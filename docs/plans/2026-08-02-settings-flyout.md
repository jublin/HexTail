# Settings flyout plan

- Add a toolbar settings flyout backed by `RadzenPanelMenu`.
- Persist global label/exclusion rules and display preferences in `AppSettings`.
- Filter excluded lines from all visible log surfaces and highlight global labels.
- Use Radzen's built-in Material light/dark runtime theming; add density and log-font controls.
- Cover persistence and rule behavior with focused tests.
